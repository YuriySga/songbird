import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  endGameBlock: {
    width: '100%',
    backgroundColor: '#303030',
    marginBottom: '25px',
    borderRadius: '4px',
  },
});
